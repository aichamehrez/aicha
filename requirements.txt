# ==============================================================================
#  OpenClaw Python Dependencies
#  Add real package names here once OpenClaw source is present.
#  This stub allows the Docker image to build cleanly right now.
# ==============================================================================

# HTTP / async
httpx>=0.27.0
aiohttp>=3.9.0

# CLI / config
python-dotenv>=1.0.0
pyyaml>=6.0.1
click>=8.1.7

# LLM & Vector Memory
openai>=1.30.0
turbovec>=0.1.0
numpy>=1.26.0
